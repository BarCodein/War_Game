# War of Dots 需求文档

## 1. 文档目的

本文档定义课程项目 **War of Dots** 的产品范围、功能需求、技术约束与 MVP 验收边界。项目玩法参考 [Steam 上的 War of Dots](https://store.steampowered.com/app/3902430/War_of_Dots/)，但代码、地图、关卡、文案、UI 与资源均须原创或使用合法占位资源。若项目公开发布，应重新评估同名带来的知识产权及产品混淆风险。

需求状态分为：**已确认**、**暂定**、**待决定**。暂定或待决定内容不得直接视为最终实现标准。

## 2. 产品目标

开发一款极简、俯视视角、2D、实时运行的桌面浏览器单机战略游戏。玩家指挥大量圆点单位，通过占领城市、生产与补给、地形利用、战争迷雾和士气管理赢得战斗。游戏应规则清楚、低视觉干扰，并在简洁机制下提供战术深度。

## 3. 平台与技术约束（已确认）

- 使用 HTML、CSS、JavaScript 与浏览器 Web API。
- 使用 Phaser 3、原生 JavaScript ES Modules、Vite 和 npm。
- 使用 Vitest 进行单元测试，Playwright 进行端到端测试。
- Node.js 仅用于开发、构建和测试；成品必须可作为纯静态 Web 应用部署。
- 不使用 TypeScript、前端 UI 框架、后端框架或非 JavaScript 技术栈的游戏引擎。
- 支持最新版 Chrome、Firefox、Edge，目标分辨率为 1280×720 至 1920×1080。
- 不要求触控设备、Safari 或原生桌面应用支持。

## 4. MVP 功能需求（已确认）

### 4.1 单位与操作

- 每个圆点表示一个独立单位，初期包含轻型和重型两类单位。
- 支持左键单选、拖动框选、Shift 增减选择、右键移动或攻击。
- 支持大量单位统一下令；输入逻辑与战斗逻辑不得紧密耦合。
- 单位属性和规则应数据驱动，避免散落的魔法数字。

### 4.2 城市、生产与补给

- 城市可生产单位、提供补给，并能被敌对阵营占领。
- 无有效敌军干扰时，附近单位逐渐增加占领进度；完成后城市改变阵营。
- 每座己方城市默认提供 5 个单位的补给容量。
- 超出补给上限的单位逐渐受到损耗，补给不足同时影响士气。
- 己方城市附近可恢复生命和士气，具体规则待确认（暂定方案见 gdd.md §7）。

### 4.3 战斗、地形与视野

- 敌对单位进入交战距离后自动攻击。
- 战斗考虑攻击距离、间隔、伤害、生命值和地形修正。
- 初始地形包括平原、森林、水域和桥梁。
- 视觉地图采用自由平面，内部可使用低分辨率逻辑网格表达通行性、寻路成本、地形和视野阻挡。
- 战争迷雾必须区分当前可见、已探索但不可见、从未探索三个状态；不可见敌军不得显示实时状态。

### 4.4 士气

- 每个单位具有士气，并受友军、城市、补给、受击和战斗结果等因素影响。
- 低士气会削弱单位；士气归零可触发溃逃，无法撤退时可投降或移除。
- 所有影响因素、阈值和修正均须配置化；具体公式仍为暂定设计。

### 4.5 关卡、控制器与胜负

- MVP 提供一个教学战役关卡，由人类玩家对抗脚本化敌军。
- 敌军脚本支持驻守、定时增援、条件触发进攻及向预设目标移动。
- 人类、脚本及未来 AI 应通过统一命令接口控制游戏世界。
- 胜利条件：占领全部敌方城市；或消灭全部敌军且敌方无可继续生产单位的城市。失败条件与其对称。
- 支持暂停与游戏速度控制。

### 4.6 地图编辑器

- 支持新建地图并设置名称、尺寸。
- 支持绘制和擦除地形，以及放置、移动、删除城市、出生点和桥梁等对象。
- 支持设置城市初始阵营，并校验基本可玩条件。
- 编辑器与运行时共享同一地图模型。
- 地图采用带版本号的 JSON，可通过 `localStorage` 保存和载入，并可通过文件 API 导入、导出。
- 支持从编辑器一键测试地图，并安全返回编辑器。

## 5. 数据、表现与性能（已确认）

- 地图、关卡和配置使用版本化 JSON；读取时执行结构校验并预留迁移机制。
- `localStorage` 保存设置、战役进度和自定义地图，不使用数据库。
- 渲染层与游戏规则分离；用户可见文本集中管理，中文优先并预留英文支持。
- 视觉采用原创极简几何风格，音频与美术资源应可替换。
- 目标是在三款支持浏览器中尽量保持 60 FPS，并支持约 500 个活动单位。
- 核心模拟采用固定时间步长，不依赖显示器刷新率；避免全单位两两检测，并建立可测量的性能基线。

## 6. MVP 验收标准

MVP 完成时，玩家应能在支持的浏览器中完成教学关卡的完整循环：选择和指挥单位、依据地形交战、观察迷雾与士气变化、占领城市、承受补给影响并触发明确胜负结果。编辑器应能创建一张有效地图，完成保存、载入、导入、导出和测试闭环。关键规则应有 Vitest 测试，主要玩家流程应有 Playwright 测试，构建产物应能静态部署。

## 7. 明确不在范围内

- 在线 PvP、在线合作、排位、匹配、大厅及任何网络后端。
- 世界模式、回放和观战。
- MVP 阶段的自主战略 AI、正式沙盒、复杂编队、完整战役和战斗中途存档。

## 8. 待确认事项

以下各项已有「暂定」方案（见 `docs/gdd.md` 相应章节），待团队确认后改为「已确认」。

- 士气公式、阈值、反馈方式以及投降或移除规则 —— 暂定方案见 gdd.md §6。
- 单位和地形参数、伤害公式、目标选择与碰撞规则 —— 暂定方案见 gdd.md §3–§5。
- 城市自动生产或玩家配置生产，以及城市恢复规则 —— 暂定方案见 gdd.md §7（MVP 采用自动生产）。
- 占领速度、争夺判定和恢复速度 —— 暂定方案见 gdd.md §7。
- 森林视野阻挡与敌军最后已知位置的表现 —— 暂定方案见 gdd.md §5、§9。
- 教学关卡内容、任务触发细节及完整可玩条件 —— 暂定方案见 gdd.md §10。
- 后续 AI、沙盒、战役、存档、美术和音频方案 —— 待决定（超 MVP 范围，MVP 完成后讨论）。

## 9. 开发顺序

先确认术语与需求，再编写游戏设计文档、技术架构文档及可验证的 MVP 验收标准。上述文档经确认后方可实现 MVP；完成测试与验收后，再逐步考虑 AI、沙盒和完整战役。
